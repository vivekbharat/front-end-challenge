// sortByRateFilter = () => {
//   const data = [...this.state.profileData];

//   data.sort(
//     (a, b) => parseFloat(b.averageRating) - parseFloat(a.averageRating)
//   );
//   data.map(item => <Details datas={item} />);
//   // console.log(data, "sorted by rate");
// };

// sortByPopularFilter = () => {
//   const data = [...this.state.profileData];
//   data.sort(
//     (a, b) => parseFloat(b.views.length) - parseFloat(a.views.length)
//   );
//   data.map(item => <Details datas={item} />);
//   // console.log(data, "sorted by popularity");
// };

// componentWillUpdate(nextProps, nextState) {
//   this.sortByRateFilter();
// }

// data.sort( a, b) => parseFloat(b.averageRating) - parseFloat(a.averageRating)).map(item => <Details datas={item} />)
